# DeepGraph Next.js Starter

This project is a simple static site built with **Next.js 14**.

To build the project locally, install dependencies and run:

```bash
npm run build
```

The build will output static files using `output: 'export'` as configured in `next.config.js`.
